%h3 Comments
- @post.comments.each do |comment|
  .well
    %p
      %strong>= comment.username
      \: #{comment.body}
      \#{link_to "[X]", [comment, comment.post], method: :delete, data: {confirm: "Are you sure?"}}
