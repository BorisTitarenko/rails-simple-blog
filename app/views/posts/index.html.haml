%h1 Blog posts
- if session[:email]
  - @posts.each do |post|
    .well
      %h3= post.title + " by " + post.user[:first_name]
      %p= post.body
      = link_to "Read more", post_path(post), :class => "btn btn-default"
